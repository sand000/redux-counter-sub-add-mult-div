import React from 'react'

const initState ={
        count:0,
        key :"value",
}

const addCounterReducer = (state=initState, action) => {
   switch(action.type){

    case "" : return {}
    case "" : return {}
    case "" : return {}
    case "" : return {}
    case "" : return {}

   }
}

export default Redu
state:initState